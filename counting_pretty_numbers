#include <iostream>
using namespace std;

int main()
{
    int t=0 , a=0, b=0 ,count=0;
    cin>>t;
    for(int i=0;i<t;i++)
    {
        cin>>a>>b;
        
        for(int j=a;j<=b;j++)
        {
            if(j%10==2 || j%10==3 || j%10==9)
            count++;
        }
        cout<<count<<endl;
        count=0;
    }

    return 0;
}
